diff --git a/app/Http/Controllers/Admin/PostController.php b/app/Http/Controllers/Admin/PostController.php
index f1bd2d7..62ef685 100644
--- a/app/Http/Controllers/Admin/PostController.php
+++ b/app/Http/Controllers/Admin/PostController.php
@@ -29,12 +29,24 @@ public function store(Request $request)
             'body'        => 'required',
             'category_id' => 'nullable|exists:categories,id',
             'thumbnail'   => 'nullable|image|max:2048',
+            // Tag dipisahkan dengan koma, contoh: "kota baru, kegiatan, kominfo"
+            'tags'        => 'nullable|string|max:255',
         ]);
 
         $thumb = $request->hasFile('thumbnail')
             ? $request->file('thumbnail')->store('thumbnails', 'public')
             : null;
 
+        $normalizedTags = null;
+        if (!empty($request->tags)) {
+            $normalizedTags = collect(explode(',', $request->tags))
+                ->map(fn ($t) => trim($t))
+                ->filter()
+                ->unique()
+                ->take(15)
+                ->implode(', ');
+        }
+
         Post::create([
             'title'        => $request->title,
             'slug'         => Str::slug($request->title),
@@ -42,6 +54,7 @@ public function store(Request $request)
             'thumbnail'    => $thumb,
             'published_at' => now(),
             'category_id'  => $request->category_id,
+            'tags'         => $normalizedTags,
         ]);
 
         return redirect()->route('admin.posts.index')
@@ -61,17 +74,29 @@ public function update(Request $request, Post $post)
             'body'        => 'required',
             'category_id' => 'nullable|exists:categories,id',
             'thumbnail'   => 'nullable|image|max:2048',
+            'tags'        => 'nullable|string|max:255',
         ]);
 
         if ($request->hasFile('thumbnail')) {
             $post->thumbnail = $request->file('thumbnail')->store('thumbnails', 'public');
         }
 
+        $normalizedTags = null;
+        if (!empty($request->tags)) {
+            $normalizedTags = collect(explode(',', $request->tags))
+                ->map(fn ($t) => trim($t))
+                ->filter()
+                ->unique()
+                ->take(15)
+                ->implode(', ');
+        }
+
         $post->update([
             'title'       => $request->title,
             'slug'        => Str::slug($request->title),
             'body'        => $request->body,
             'category_id' => $request->category_id,
+            'tags'        => $normalizedTags,
         ]);
 
         return redirect()->route('admin.posts.index')
diff --git a/app/Http/Controllers/Writer/PostController.php b/app/Http/Controllers/Writer/PostController.php
index 5594135..cbe7973 100644
--- a/app/Http/Controllers/Writer/PostController.php
+++ b/app/Http/Controllers/Writer/PostController.php
@@ -35,6 +35,8 @@ public function store(Request $request)
             'body' => ['required','string'],
             'thumbnail' => ['nullable','image','max:4096'],
             'status' => ['required','in:draft,publish'],
+            // Tag dipisahkan dengan koma, contoh: "kota baru, kegiatan, kominfo"
+            'tags' => ['nullable','string','max:255'],
         ]);
 
         $thumbnailPath = null;
@@ -42,6 +44,17 @@ public function store(Request $request)
             $thumbnailPath = $request->file('thumbnail')->store('thumbnails','public');
         }
 
+        // Normalisasi tag: trim, buang yang kosong, hapus duplikat, gabungkan lagi
+        $normalizedTags = null;
+        if (!empty($data['tags'] ?? null)) {
+            $normalizedTags = collect(explode(',', $data['tags']))
+                ->map(fn ($t) => trim($t))
+                ->filter()
+                ->unique()
+                ->take(15)
+                ->implode(', ');
+        }
+
         Post::create([
             'title' => $data['title'],
             'slug' => Str::slug($data['title']).'-'.Str::random(5),
@@ -50,6 +63,7 @@ public function store(Request $request)
             'thumbnail' => $thumbnailPath,
             'published_at' => $data['status'] === 'publish' ? now() : null,
             'user_id' => $request->user()->id, // ✅ penting biar “punya siapa”
+            'tags' => $normalizedTags,
         ]);
 
         return redirect()->route('writer.dashboard')->with('status', 'Berita berhasil disimpan.');
@@ -73,6 +87,7 @@ public function update(Request $request, Post $post)
         'body' => ['required','string'],
         'thumbnail' => ['nullable','image','max:4096'],
         'status' => ['required','in:draft,publish'],
+        'tags' => ['nullable','string','max:255'],
     ]);
 
     // upload thumbnail kalau ada
@@ -84,6 +99,18 @@ public function update(Request $request, Post $post)
     $post->excerpt = $data['excerpt'] ?? $post->excerpt;
     $post->body = $data['body'];
 
+    // normalisasi tag
+    if (array_key_exists('tags', $data)) {
+        $post->tags = !empty($data['tags'])
+            ? collect(explode(',', $data['tags']))
+                ->map(fn ($t) => trim($t))
+                ->filter()
+                ->unique()
+                ->take(15)
+                ->implode(', ')
+            : null;
+    }
+
     // publish logic
     $post->published_at = $data['status'] === 'publish'
         ? ($post->published_at ?? now())
diff --git a/app/Models/Post.php b/app/Models/Post.php
index ac88f67..aad553d 100644
--- a/app/Models/Post.php
+++ b/app/Models/Post.php
@@ -11,7 +11,7 @@ class Post extends Model
     use HasFactory;
 
     protected $fillable = [
-        'title','slug','excerpt','body','thumbnail','published_at','user_id'
+        'title','slug','excerpt','body','thumbnail','published_at','user_id','tags'
     ];
 
     protected $casts = [
diff --git a/resources/views/Writer/posts/create.blade.php b/resources/views/Writer/posts/create.blade.php
index 3cc7ce2..b7df63e 100644
--- a/resources/views/Writer/posts/create.blade.php
+++ b/resources/views/Writer/posts/create.blade.php
@@ -41,6 +41,13 @@
       @error('status')<div class="text-sm text-red-600 mt-1">{{ $message }}</div>@enderror
     </div>
 
+    <div>
+      <label class="block text-sm font-medium mb-1">Tag (opsional)</label>
+      <input name="tags" class="w-full border rounded-lg px-3 py-2" value="{{ old('tags') }}" placeholder="Contoh: kota baru, kegiatan, kominfo">
+      <p class="text-xs text-slate-500 mt-1">Pisahkan beberapa tag dengan koma.</p>
+      @error('tags')<div class="text-sm text-red-600 mt-1">{{ $message }}</div>@enderror
+    </div>
+
     <div class="pt-2">
       <button class="px-4 py-2 rounded-lg bg-blue-600 text-white">Simpan</button>
       <a href="{{ route('writer.dashboard') }}" class="ml-2 px-4 py-2 rounded-lg border">Batal</a>
diff --git a/resources/views/Writer/posts/edit.blade.php b/resources/views/Writer/posts/edit.blade.php
index 2f4a2f5..1859479 100644
--- a/resources/views/Writer/posts/edit.blade.php
+++ b/resources/views/Writer/posts/edit.blade.php
@@ -54,6 +54,14 @@ class="mt-1 w-full rounded-lg border px-3 py-2" required>{{ old('body', $post->b
       </select>
     </div>
 
+    <div>
+      <label class="block text-sm font-medium">Tag (opsional)</label>
+      <input name="tags" value="{{ old('tags', $post->tags) }}"
+             class="mt-1 w-full rounded-lg border px-3 py-2" placeholder="Contoh: kota baru, kegiatan, kominfo">
+      <p class="text-xs text-slate-500 mt-1">Pisahkan beberapa tag dengan koma.</p>
+      @error('tags') <div class="text-sm text-red-600 mt-1">{{ $message }}</div> @enderror
+    </div>
+
     <div class="flex items-center gap-3 pt-2">
       <button class="px-4 py-2 rounded-lg bg-blue-600 text-white">Simpan Perubahan</button>
       <a href="{{ route('writer.dashboard') }}" class="px-4 py-2 rounded-lg border">Batal</a>
diff --git a/resources/views/admin/posts/create.blade.php b/resources/views/admin/posts/create.blade.php
index e4758aa..0783425 100644
--- a/resources/views/admin/posts/create.blade.php
+++ b/resources/views/admin/posts/create.blade.php
@@ -41,6 +41,13 @@
       @error('status')<div class="text-sm text-red-600 mt-1">{{ $message }}</div>@enderror
     </div>
 
+    <div>
+      <label class="block text-sm font-medium mb-1">Tag (opsional)</label>
+      <input name="tags" class="w-full border rounded-lg px-3 py-2" value="{{ old('tags') }}" placeholder="Contoh: kota baru, kegiatan, kominfo">
+      <p class="text-xs text-slate-500 mt-1">Pisahkan beberapa tag dengan koma.</p>
+      @error('tags')<div class="text-sm text-red-600 mt-1">{{ $message }}</div>@enderror
+    </div>
+
     <div class="pt-2">
       <button class="px-4 py-2 rounded-lg bg-blue-600 text-white">Simpan</button>
       <a href="{{ route('admin.dashboard') }}" class="ml-2 px-4 py-2 rounded-lg border">Batal</a>
diff --git a/resources/views/posts/show.blade.php b/resources/views/posts/show.blade.php
index 0466180..3f913c0 100644
--- a/resources/views/posts/show.blade.php
+++ b/resources/views/posts/show.blade.php
@@ -15,6 +15,22 @@
       {{ $post->published_at?->format('d M Y H:i') }}
     </div>
 
+    @if(!empty($post->tags))
+      @php
+        $tags = collect(explode(',', $post->tags))
+          ->map(fn ($t) => trim($t))
+          ->filter();
+      @endphp
+
+      @if($tags->isNotEmpty())
+        <div class="mt-3 flex flex-wrap gap-2">
+          @foreach($tags as $tag)
+            <span class="text-xs px-2 py-1 rounded-full border bg-slate-50 text-slate-700">{{ $tag }}</span>
+          @endforeach
+        </div>
+      @endif
+    @endif
+
     @if($post->thumbnail)
       <div class="mt-6 rounded-2xl overflow-hidden border bg-slate-100">
         <img src="{{ Storage::url($post->thumbnail) }}" class="w-full object-cover" alt="{{ $post->title }}">
